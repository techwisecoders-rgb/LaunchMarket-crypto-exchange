# Railway Nixpacks config
# Ensures the whole monorepo is installed so workspace packages resolve

[phases.setup]
nixPkgs = ["nodejs-20_x", "npm-10_x"]

[phases.install]
cmds = ["npm install --workspaces --include-workspace-root"]

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"